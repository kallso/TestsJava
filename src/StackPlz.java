import java.util.ArrayDeque;
import java.util.Deque;

public class StackPlz {

    public static void main(String[] args) {
        Deque<Integer> stack = new ArrayDeque<>();
        // LIFO<Integer> stack = new ArrayLIFO<>();

        //stack.
        // Objects.equals(o, e)
    }
}
/*
interface LIFO<E> extends Collection<E> {
    E peek();

    E pop();

    void push(E e);

    Properties
}

class ArrayLIFO<E> extends ArrayDeque<E> implements LIFO<E> {}
*/

/* class LIFO implements */
